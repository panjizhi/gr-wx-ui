<view class="index" wx:if="{{ready}}">
    <view class="user-infos">
        <image class="user-logo" src="{{user.avatarUrl}}"></image>
        <view class="user-name">{{user.name || user.nickName}}</view>
    </view>

    <view wx:if="{{papers.length}}" class="weui-panel">
        <view class="weui-panel__bd">
            <view class="weui-media-box weui-media-box_small-appmsg">
                <view class="weui-cells weui-cells_in-small-appmsg">
                    <navigator
                        wx:for-items="{{papers}}"
                        wx:key="{{item._id}}"
                        url="/page/exam/index?id={{item._id}}&dispatchId={{item._dispatchId}}"
                        class="weui-cell weui-cell_access"
                        hover-class="weui-cell_active">
                        <view class="weui-cell__bd weui-cell_primary">
                            <view>{{item.name}}</view>
                        </view>
                        <view class="weui-cell__ft weui-cell__ft_in-access"></view>
                    </navigator>
                </view>
            </view>
        </view>
    </view>

    <view wx:else class="no-data">
        <view class="no-data-tips">暂无数据</view>
    </view>
</view>
